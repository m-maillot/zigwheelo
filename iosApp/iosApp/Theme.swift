import Foundation
import SwiftUI

class ZWColors {
    static let primary: Color = Color(UIColor(named: "primary")!)
    static let secondary: Color = Color(UIColor(named: "secondary")!)
    static let background: Color = Color(UIColor(named: "background")!)
}

struct ZWFonts {
    let body: Font
}
